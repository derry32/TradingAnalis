#!/usr/bin/expect -f
set timeout 120
spawn ssh -o StrictHostKeyChecking=no ubuntu@43.156.79.235 "cd ~/TradingAnalis && sudo docker rm -f ai-frontend && sudo docker run -d --name ai-frontend --restart always -p 3000:3000 --env-file ./frontend/.env.local ai-trading-frontend"
expect {
    "password:" {
        send "thunder-29%-quantum\r"
        exp_continue
    }
    eof
}
