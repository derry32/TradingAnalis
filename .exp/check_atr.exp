#!/usr/bin/expect -f
set timeout 60
spawn ssh -o StrictHostKeyChecking=no ubuntu@43.156.79.235 "sudo docker logs --tail 500 ai-backend 2>&1 | grep 'ATR' | head -20"
expect {
    "password:" { send "thunder-29%-quantum\r"; exp_continue }
    eof
}
