#!/usr/bin/expect -f
set timeout 60
spawn ssh -o StrictHostKeyChecking=no ubuntu@43.156.79.235 "sudo docker exec -u root ai-mt5-vnc bash -c \"chown -R kasm-user:kasm-user /headless && su - kasm-user -c 'DISPLAY=:1 WINEPREFIX=/headless/.wine wine /tmp/mt5setup.exe &'\""
expect {
    "password:" {
        send "thunder-29%-quantum\r"
        exp_continue
    }
    eof
}
