#!/usr/bin/expect -f
set timeout 10
spawn ssh -o StrictHostKeyChecking=no ubuntu@43.156.79.235 "sudo docker logs ai-backend | grep HYPER_SCALPER | tail -n 10"
expect {
    "password:" {
        send "thunder-29%-quantum\r"
        exp_continue
    }
    eof
}
